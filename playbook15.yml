---
- name: tagging in ansible
  hosts: all
  tasks:
   - name: install tree
     apt:
      name: install tree
      state: present
     tag: tree install
   - name: creation user
     user: 
      name: ranu
      password: janu
     tag: user creat
   - name: copy/etc/password file
     copy:
      src: /etc/password
      dest: /tmp
